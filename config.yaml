app:
  name: "go-microservice"
  env: "development"

http:
  port: 8080
  timeout: "30s"

grpc:
  port: 9090

db:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "password"
  name: "mydb"
  sslmode: "disable"

logger:
  level: "info"
